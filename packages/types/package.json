{
  "name": "@solarnexus/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "lint": "tsc --noEmit",
    "dev": "tsc --build --watch"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "typescript": "^5.4.5"
  }
}
